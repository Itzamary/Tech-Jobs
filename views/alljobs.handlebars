 <li class="btn-no-style"><a href="'/addJob" id="addJob">Add-Job</a></li>
<div class="job-posts">
<h3> All available Jobs</h3>
<ol class="post-list">
  {{#each posts}}
    <li>
      <article class="post">
        <div class="title">
          <div>{{title}}</div>
          <div>
            Created by:
            {{user.username}}
            on
            {{format_date created_at}}
          </div>
          <div>
            {{title_body}}
          </div>
        </div>
        <div class="post"> Budget: ${{budget}} 
        <button href="{{post_url}}" target="_blank">Apply Now</button>
        </div>
        <div>Technology Needed: {{techs}} </div>
        <div>
          Futher questions can be asked here
          <span> Email: {{user.email}} </span>
        </div>
        <a href="/post/{{id}}">{{comments.length}} {{format_plural "review" comments.length}}</a>
      </article>
    </li>
  {{/each}}
</ol>
</div>